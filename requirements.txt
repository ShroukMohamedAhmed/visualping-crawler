playwright>=1.45.0
beautifulsoup4>=4.12.0
pillow>=10.0.0

# Optional: only needed for .woff2 fonts (brotli-compressed internally)
# and `Content-Encoding: br` responses. Everything works without it.
brotli>=1.1.0
